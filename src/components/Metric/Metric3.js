import classes from "./Metrics.module.css"

export const Metric3 = () => {
    return (
    <div className={classes.Box}>
        <p className={classes.quantity}>
            1.4K
        </p>
        <p className={classes.type}>Photos</p>
    
    </div>
    )
};